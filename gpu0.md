# gpu0 矩阵乘法测速

## 目的
量显卡算一次 4096×4096 矩阵乘法要多久、占多少显存。

## 做法
在显卡上造两个随机矩阵，相乘 20 遍，记总时间；再用 max_memory_allocated 记峰值显存。
第二版改为200遍

## 第一版的问题
没写 torch.cuda.synchronize()，量到的是派活时间，不是算完时间。
20 次共 0.262 秒，平均每次 13.1 毫秒（偏低）

## 加上同步后
200 次共 3.162 秒，平均每次 15.8 毫秒
峰值显存 264.1 MB

## 结论
量显卡的耗时必须写同步，否则数字偏低。样本也要够大，20 次太少。