# 同一个模型换运行时的对比

## 目的
上一轮用 pytorch 跑 Qwen3-0.6B，生成速度只有 10.8 token/秒，查出来是 CPU 在发指令上耗掉了大半时间。但一个数不算证据，得换个运行时跑同一个模型，才能确认慢在软件还是硬件。

## 做法
llama.cpp 用官方预编译的 CUDA 版（b11060，对应 CUDA 13.4），模型用官方 GGUF 的 Q8_0 版本，604 兆，全部层放显卡（`-ngl 99`）。速度用它自带的 llama-bench 量，不用自己写脚本。

## 结果
- pp512：15990 token/秒（一次喂 512 个 token 的速度）
- tg128：270.85 token/秒（一个一个往外吐的速度）
- 对照：pytorch 那边 10.8 token/秒

同一个模型、同一张卡、同样接着电，差 25 倍。

## 为什么差这么多
llama.cpp 是 C++ 写的，算子合并过，每一步的开销小；pytorch 那边每一步要经 Python 发八百多条指令，CPU 占满、显卡闲着。

有一处不完全公平要说清楚：llama.cpp 用的是 Q8_0 量化权重，pytorch 那边是 bf16，精度不一样。但这个差别解释不了 25 倍，大头还是发指令的开销。

另外，把之前那段矩阵乘压满重跑：时钟 2505 MHz、功耗 139 瓦、9.4 TFLOPS，跟昨晚一样，中间撞到过一次 140 瓦的功耗墙。说明这张卡的算力没问题。生成时只跑到 780 MHz，是因为活太小太碎，显卡懒得提频。

## 总结
- 用 pytorch 跑 Qwen3-0.6B，生成速度只有 10.8 token/秒，查出来是 CPU 在发指令上耗掉了大半时间。