# Qwen3-0.6B 本机跑通

## 目的
在本机显卡上跑一个开源小模型，看加载要多久、生成多快、占多少显存。

## 做法
用 transformers 加载 Qwen3-0.6B，bfloat16，搬到显卡。问它一句"用一句话解释什么是显存"，最多生成 100 个 token。加载和生成前后各记一次时间，显存用 max_memory_allocated 量峰值。

## 结果
权重 1.5G，参数量 0.6B。

第一次跑，加载 76.87 秒。但是这个数里包含了下载权重的部分。
第二次跑，加载 8.73 秒，这个才是真正的加载。

生成速度，第一次 19 个 token 用 2.78 秒，每秒 6.8 个；第二次 24 个 token 用 2.22 秒，每秒 10.8 个。
峰值显存 1157 MB，权重 1.2G，对得上。这轮上下文短，KV 缓存还没长起来。

## 为什么速度只有10.8 tokens/s
这个大小的模型正常能跑几十到几百，10.8 偏慢。

经过deepseek大人查证，Qwen3模型回答那一下总共花了 90 毫秒，其中 85 毫秒是 CPU 在忙，显卡真正算的只有 17 毫秒，八成时间在干等。原因是模型每走一步都要往显卡丢八百多个小活儿，每个来回几十微秒，攒起来 CPU 就占满了，显卡反而闲着。

